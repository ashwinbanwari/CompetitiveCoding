#include <bits/stdc++.h>

using namespace std;

int main() {
	ios_base::sync_with_stdio(0); cin.tie(0);

    int m, d, l, r;
    cin >> m >> d >> l >> r;
    vector<int> dp(r+1);
    dp[l] = 

	return 0;
}